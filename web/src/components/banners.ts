export const handleBannerClick = (bannerName: string) => {
    console.log(`Clic en el banner de: ${bannerName}`);
    // Aquí podemos enviar el evento a un backend para registrar la interacción. Para futuras versiones
  };
  
export const handleMouseEnter = (bannerName: string) => {
    console.log(`Mouse ha pasado sobre el banner de: ${bannerName}`);
    // Esto se puede usar para alguna animación o registro. Para futuras versiones
  };